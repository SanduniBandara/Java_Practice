class Area_of_Triangle {
  public static void main (String [] args){
    int b = 12,h = 6, area;
    area = (b*h)/2;
    System.out.println("Area of the Triangle given = " + area);
    /* Here area is set to an integer variable whereas the base and height
    are defined as integers and given a value.
    Also the output is displayed in this program */
    
  }
}
